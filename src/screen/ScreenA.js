import React from 'react'
import { Text, View } from 'react-native'

const ScreenA = () => {
	return (
		<View>
			<Text>
				In IntelliJ IDEA, a library is a file or a set of files whose functions
				and methods are added to IntelliJ IDEAs internal knowledge in addition
				to the functions and methods that IntelliJ IDEA retrieves from the
				project code that you edit. In the scope of a project, its libraries by
				default are write-protected.
			</Text>
		</View>
	)
}

export default ScreenA
